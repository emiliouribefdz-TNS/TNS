## Estructura principal

La aplicacion activa ahora vive directamente en esta carpeta: `/Users/emiliouribe/TNKR`.

Archivos importantes:

- `CLAUDE.md`: archivo de referencia
- `package.json`: dependencias y scripts reales del proyecto
- `app/`: codigo fuente principal

## Lo que se organizo

Se hizo esta limpieza:

- el contenido de `tnkr-app/` se movio a la raiz
- la carpeta intermedia `tnkr-app/` se elimino porque ya no hacia falta
- los archivos sueltos antiguos de la raiz quedaron guardados en `respaldo-raiz/`

## Como trabajar

Ahora puedes correr el proyecto directamente desde esta carpeta.

```bash
cd /Users/emiliouribe/TNKR
npm run dev
```
